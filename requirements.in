Django~=3.2
django-elasticsearch-dsl-drf
django-environ
django-helusers
# 0.10.0 is needed because 1.0.0 switches to a more strict
# JWT validation library. Our "Tunnistamo"-implementation
# generates invalid amr claims in tokens.
drf-oidc-auth==0.10.0
pytz
psycopg2
# openpyxl 3.1.1 has a bug that breaks the TOS importer,
# so we need to pin it to 3.1.0 until 3.1.2 is released.
# See: https://foss.heptapod.net/openpyxl/openpyxl/-/issues/1963
openpyxl==3.1.0
djangorestframework
django-cors-headers
django-filter
django-admin-json-editor
django-admin-sortable2
djangorestframework-xml
lxml
sentry-sdk
social-auth-app-django
